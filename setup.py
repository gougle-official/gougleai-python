from setuptools import setup

setup(
    name='gougleai',
    version='1.12',
    packages=['gougleai'],
    install_requires=[

    ],
    entry_points={
        'console_scripts': [

        ],
    },
)
